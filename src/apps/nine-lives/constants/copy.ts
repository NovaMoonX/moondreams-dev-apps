/** Shared UI copy for Nine Lives — add more here as strings need to stay consistent across components, rather than one-off files per feature. Keep names explicit about what they label, not just what feature they belong to. */

/** Label for the action that marks an upcoming visit as done — shared by the "needs attention" dashboard and the Visits section's timeline so they can't drift apart. */
export const MARK_VISIT_DONE_LABEL = 'Mark as done';
